# @opentui/* is pinned to the version the opencode runtime actually ships
# (see README "Hacking on it"), which conflicts with @opencode-ai/plugin's
# declared peer range. Peers are resolved by the opencode host at runtime,
# not by this package, so skip npm's strict peer resolution.
legacy-peer-deps=true
